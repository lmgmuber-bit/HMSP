# Django Settings
DJANGO_SECRET_KEY=django-insecure-change-this-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Email Configuration (Para Gmail)
# 1. Crea una contraseña de aplicación en: https://myaccount.google.com/apppasswords
# 2. Usa esa contraseña en EMAIL_HOST_PASSWORD
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu-correo@gmail.com
EMAIL_HOST_PASSWORD=tu-password-de-aplicacion
DEFAULT_FROM_EMAIL=noreply@hmsp.cl

# Para desarrollo, puedes usar console backend (muestra emails en consola)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
